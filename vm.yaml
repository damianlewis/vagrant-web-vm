---
ip: "192.168.10.10"

folders:
    - map: ~/code/site
      to: /vagrant

sites:
    - map: site.dev
      to: /vagrant

databases:
    - name: site

# Additional PHP modules to install
#php-modules:
#    - php7.0-curl

# Global Composer packages to install
#composer-packages:
#    - laravel/envoy

# Global npm packages to install
#npm-packages:
#    - gulp-cli